import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: [
      "nikeco.vtexassets.com",
      "www.accesoriosparacomputadores.co",
  "encrypted-tbn0.gstatic.com",
  // puedes agregar más dominios aquí si los necesitas
    ],
  },
};

export default nextConfig;
